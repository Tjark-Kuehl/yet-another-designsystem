{
  "name": "yet-another-designsystem",
  "private": true,
  "scripts": {
    "hot": "turbo run hot --parallel",
    "build": "turbo run build --summarize",
    "build:ci": "turbo run build --output-logs=errors-only",
    "build:force": "turbo run build --no-cache --force",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.6"
}
